use serde::Deserialize;

#[derive(Deserialize)]
pub struct Input {
    pub arr: Vec<i64>,
}

pub fn solve(arr: &[i64]) -> i64 {
    // TODO: return the maximum contiguous subarray sum
    let _ = arr;
    todo!()
}
