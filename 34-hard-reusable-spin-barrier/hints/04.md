# The rotten reference

`rotten/main.c` is the trap: it uses one cumulative count with no phase marker,
so a thread looping into the next phase sees the already-satisfied count and is
released early. It passes a one-shot check and fails on reuse.

Next: [05.md](05.md)
