
## 06:38 2026-07-26

Pruned BUGS.md: all four open entries were resolved and are now closed out.

- T1 — Python case harnesses passing on empty case lists (fix 9e500ba)
- T2 — golden/rotten subprocess tests ignoring a failed exit code (fix 9e500ba)
- T3 — challenge 26's Go harness fabricating expectations after I/O errors (fix 9e500ba)
- T4 — 22's golden/main.py was the unimplemented stub itself; implemented the
  eight generators (naturals/sieve/primes/fibonacci/running_average/collatz/
  zipWith/unfold) and verified against python/test_solution.py's 11 behavioral
  assertions via a direct module load, since pytest's own rootdir insertion
  shadows golden/ with the python/ stub if you naively point PYTHONPATH at
  golden/ and let pytest import through the python/ test file (fix e8c541c)

Repo-wide `make test` now passes with zero failures. BUGS.md is empty.

## Later 2026-07-26 — relicense, source verification, codex audit

Relicensed the repo from the Unlicense (public domain) to GPL-3.0 (`87c437d`):
canonical LICENSE from gnu.org, UNLICENSE removed, README updated. Added a NOTICE
crediting the upstream problem sets and reference works per the credits skill.
Verified all 49 unique citation URLs across every README/HINTS resolve; the one
dead link (Fidge's vector-clock paper, lth.se mirror 404) was repointed to its
Semantic Scholar entry. Remaining non-200s are bot-blocked publishers.

Correction to the 07-25 entry: `make sys`/`make sys-rotten` DO run here. The sys
Makefiles' `CC ?= gcc` is a no-op against Make's built-in `CC=cc` default (origin
`default`), so they build with clang. Verified 6/6 stress pass and 6/6 controls
fail as intended. Memory `toolchain-no-gcc` corrected too.

Ran a codex adversarial audit over all 57 challenges and reproduced each finding
before recording 12 entries (C1–CC) in BUGS.md — record-only, nothing fixed. Two
are high: 48's golden prints a wrong answer and crashes with StopIteration on
valid input, and 41's golden `range_count` is O(n) per query so it times out at
the documented 10⁵-op max. The rest are medium/low: unbounded coin lists (07),
a rotten wrong on a small input (45), 31's C capacity being half the documented
1<<22, out-of-domain large fixtures (43, 48), a false-positive in 30's stress
(value 0 overloaded as "torn"), the shared C harness trimming trailing whitespace
while bench compares byte-exact, and the T1/T3 false-green harness class
reappearing in 53–57 (added after the 9e500ba fix).
