---
summary: |
  Personal coding-practice challenge bench. Cleared the BUGS.md queue; released v0.1.5.
  - v0.1.5: 11 Go + 1 C scaffold int64 fixes, sys 29-34 Makefile standardization, doc-neutrality touch-ups
  - open (deferred): 04 slug/title mismatch (coordinated rename); 14 hints/ lack a Sources file
  - open (by-design): bench recipes don't fully punish sort/heuristic (58/59); documented in hints
  - env: make clean's cargo step can't wipe shared /tmp/cargo-target (EPERM) — pre-existing, not a regression
---

## 07:14 2026-08-24

Split every I/O Go solver (and template) so `solve` lives alone in `solution.go`
and `main.go` is pure JSON IO — mirrors the Rust lib/main layout. AST-position
splitter (byte-accurate, gofmt), fable-refined (caught 44's BLOSUM data left in
main.go). All 57 build+vet clean. Spec: `specs/go-solver-layout.md`.

Publish gates re-verified green end to end: make test, sys, sys-rotten, golden,
rotten (56/56 traps time out), cases (114 reproducible).

## Later 2026-08-24 — released v0.1.4

Finalization was blocked for ~a day by an environment fault: running as `ondra`
but `HOME=/home/dockbox` with a root-owned, empty `~/.gnupg`, so `commit.gpgsign`
made every commit fail. Once HOME/keyring were repaired, committed the pending
tree in 9 coherent chunks — go split; new challenges 58–65; remove challenge 40;
HINTS.md→hints/ migration; fix(24) LRU; README clarity pass; seeded cases;
defect log; CLAUDE.md — then cut and signed **v0.1.4** at `ea87f54`.

## 12:15 2026-08-24

Cleared the BUGS.md queue and cut v0.1.5 (9 commits). Biggest: 11 Go + 1 C
solver scaffolds narrowed i64 fields to `int` (overflow on GOARCH=386) — widened
main.go/solution.go/test together. Added int64-boundary fixtures to 01/02/28/49/52;
golden and rotten agree on every new `.out`.

## 12:30 2026-08-24

Also shipped: sys 29-34 Makefile standardization (CC ?= cc, bare TIMEOUT, rust
bench builds before timing, help/clean), 22 golden value tests, 37 rust accessor
drop, README/hint solution-neutrality fixes. Opus reverified the judgment-call
edits (clean); sonnet numbering sweep found 14 hints/ lack a Sources file → logged,
don't fabricate citations.

