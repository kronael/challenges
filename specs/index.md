# Specs

Design specifications for the challenge bench.

- [go-solver-layout](go-solver-layout.md) — Go I/O solvers split into
  `solution.go` (solve only) + `main.go` (IO scaffold), mirroring Rust's
  lib/main split.
