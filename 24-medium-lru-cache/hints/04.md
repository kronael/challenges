# Complexity

The trap is any approach that scans the cache to find the eviction victim.
Stamping each key with a "last used" tick and scanning for the smallest stamp on
every overflow is what `rotten/main.py` does. It is correct, but O(capacity) per
eviction makes the whole run O(n·capacity), so it TIMEOUTs on the large cases.
